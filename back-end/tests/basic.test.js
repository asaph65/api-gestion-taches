// tests/basic.test.js - Tests de base
const request = require("supertest");

describe("Tests de base de l'API", () => {
  // Nous implémenterons les tests plus tard
  // quand nous aurons installé Jest ou Mocha
  test("Devrait passer un test basique", () => {
    expect(1 + 1).toBe(2);
  });
});
